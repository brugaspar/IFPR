import styled from "styled-components"

export const Container = styled.div`
    float:left;
    left: 0rem;

    .pgSelector{
        background: var(--shape-dark);
        border:none;
        color:darkgray;
        margin-left:0.4rem;
        margin-right:0.4rem;

        .pgSelectorOpt{
            border:none;
        }
    }


`